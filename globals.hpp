//Defininfo algumas configurações
#ifndef GLOBAIS_HPP
#define GLOBAIS_HPP

const float width = 400;
const float height = 800;
const float left = -4,right = 4,bottom = 0, top = 2*(right-left); // tela


const float raioBola = 0.3;
const float heightPlataforma = 0.2;
const float widthPlataforma = 1;

const float timeSimulation = 10;

#define DISCRETE 1
#define SHOW_SCREEN 1

#endif